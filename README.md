# REFORMAT FILE PACKAGE

Generate **REFORMAT APP**

1. Install:

```
pip install reformat-app
```

1. Create Config Folder 

2. Generate code python:

```python
from reformat_app import main

try:
    main()
except Exception as err:
    print(err)